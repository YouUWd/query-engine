package com.example.schoolquery.metadata;

/** Logical field type used by the platform-neutral result metadata contract. */
public enum SysFieldType {
    STRING, NUMBER, BOOLEAN, DATE, DATETIME, JSON, UNKNOWN
}
